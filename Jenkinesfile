pipeline {
  agent any

stages {

  stage('clone') {
    steps {
        git url: 'https://github.com/MohammedAlishan/jenkins-simple-demo/new/main'
            branch: 'main'
    }
}

stage('Run Script')
  steps {
      sh 'chmod +x script.sh'
      sh './script.sh'
      }
    }
  }
}

